MERLIN → JARVIS UPGRADE PLAN
=============================
Updated: session 2 — building voice loop

WHAT EXISTS (session 1 skeleton):
  audio/buffer.py       — RingBuffer 10min ✓
  audio/transcriber.py  — openai-whisper transcription ✓
  audio/wake.py         — regex wake word detector ✓
  audio/storage.py      — chunk storage + weekly cleanup ✓
  desktop/config.py     — config manager ✓
  desktop/main.js       — Electron transparent overlay ✓
  desktop/renderer/     — HUD UI (transcription, nav, AI panels) ✓
  desktop/system/       — permissions, sudo ✓
  server/server.py      — WS server, handles phone + HUD clients ✓
  server/stream_proc.py — batches sensor data, on_wake_command ✓

WHAT'S MISSING (building now):
  [DONE] audio/mic.py      — PC mic recording loop → whisper → wake → query server
  [DONE] audio/tts.py      — edge-tts async speak(text)
  [DONE] server/server.py  — TTS hook: speak every AI response
  [DONE] ai/tools.py           — haversine fix for find_places
  [DONE] vision/detector.py   — YOLO11n detect_sync/detect, DetectionResult.to_context()
  [DONE] stream_processor.py  — _detect_objects() + ANALYZE-only raw frame to LLM
  [DONE] ai/memory.py         — ChromaDB EpisodicMemory, store/query/prune
  [DONE] ai/session.py        — memory recall injected before query, store obs+qa
  [DONE] ai/tools.py          — search_memory tool (AI can query own memory)
  [ ] face_recognition  — enroll + tag known faces in context stream
  [ ] HA tool           — home_assistant_call(service, entity)
  [ ] Calendar tool     — Google Calendar API, proactive schedule

VOICE LOOP ARCHITECTURE:
  sounddevice → RingBuffer (2s chunks)
    → Whisper (local, run_in_executor)
      → WakeWordDetector.check()
        → session.query(command)  [direct, same process]
          → AI response
            → broadcast to HUD clients
            → edge-tts speak(response)

BUILD ORDER THIS SESSION:
  1. audio/tts.py
  2. audio/mic.py
  3. server/server.py — integrate mic + tts
  4. Test voice loop end-to-end

KNOWN ISSUES TO FIX:
  - find_places() uses flat Euclidean dist (wrong) → needs haversine
  - shell safety only blocks 4 patterns (trivial bypass)
  - openai-whisper in transcriber.py is blocking (use run_in_executor)

---

SESSION 4: Widgets + voice commands + Even G2-style tools (2026-05-06)

Implemented:
- ai/widgets.py: 12-widget registry, per-mode assignment (max 4), tiredness heuristic,
  runtime data store, resolve() for display strings
- audio/command.py: voice command parser — up/down, show [widget], switch to [mode],
  how tired am I, show todos/spending/calendar, note this, summarize conversation,
  incognito mode. Falls through to LLM if no match.
- ai/tools.py: 9 new tools — log_sleep, log_expense, get_tiredness, get_todo_list,
  add_todo, complete_todo, log_prep_note, conversation_summary, get_conversation_cue,
  memory_search, memory_save (28 total tools)
- desktop/widgets/left_panel.py: context strip renderer — shows up to 4 widgets
  per mode below the status section, auto-refreshes
- desktop/screens/main_screen.py: voice commands wired into voice loop (local actions
  skip LLM), mode switching refreshes context strip, widget refresh timer (30s)

Even G2 patterns replicated:
- Dashboard widgets → context strip (12 data sources, user-configured per mode)
- AI Cues → get_conversation_cue() tool
- Prep Notes → log_prep_note() tool
- AI Summary → conversation_summary() tool
- Even AI voice → voice command parser (up/down/show/switch)
- Health → tiredness heuristic + Fitbit integration planned
- QuickList → add_todo / complete_todo / get_todo_list

Next planned:
- Integration framework (weather, calendar, fitbit, stripe)
- Teleprompter NOTES mode widget
- Dual-view translate in TALK mode

- Sign: Claude


SESSION 5: TUI bugfixes + wake word config (2026-05-06)

Fixes:
- InputBar crashes: QuerySubmitted/MicPressed now inherit Message
  with proper super().__init__()
- Voice loop crash on startup: 1s widget mount delay, try/except
  around left panel access, graceful skip if widget unavailable
- Wake word not working: was caused by missing openai-whisper
  (transcription silently returned empty). Installed.
- Wake word editable: launch screen now has wake word input field,
  saved to ~/.merlin/desktop_config.json
- Webcam optional: START button no longer blocked by missing camera
- ModeSelector compose fix: yield TabPane directly, no with self:

New/updated files:
- desktop/screens/launch_screen.py — wake word input, optional webcam
- desktop/widgets/input_bar.py — Message inheritance
- desktop/widgets/mode_selector.py — direct TabPane yield
- desktop/screens/main_screen.py — safe widget access in voice loop

- Sign: Claude


SESSION 6: Frame relevance, power levels, caveman mode (2026-05-06)

Implemented:
- ai/frames.py: frame relevance system — saves JPEG + metadata, scores by
  tap/mentions/dossier matches/keywords, prunes to top N, marks favorites
- ai/powers.py: power level engine — social/economic/aesthetic/intellectual
  estimates, adaptive (confidence grows with each observation), caveman bar display
- ai/tools.py: 43 tools — added search_frames() and estimate_power_level()
- ai/system_prompt.txt: caveman mode rules — minimal tokens by default,
  full eloquence only for genuine human connection. No greetings, no farewells.
- desktop/screens/main_screen.py: auto-save webcam frames every 10 captures
- TOOL_DEFINITIONS: JSON defs for search_frames + estimate_power_level

New files:
- ai/frames.py
- ai/powers.py

Modified:
- ai/tools.py (+2 tools, 43 total)
- ai/system_prompt.txt (caveman mode rules)
- desktop/screens/main_screen.py (frame auto-save)

- Sign: Claude


SESSION 7: Desktop environment — terminal multiplexer + Merlin copilot (2026-05-06)

Implemented:
- desktop/terminal/pty.py: PTY manager — openpty, fork shell, async I/O
- desktop/terminal/emulator.py: pyte-based VT100 terminal emulator with
  2D cell buffer, ANSI color mapping, plain text extraction
- desktop/widgets/terminal_pane.py: Textual widget rendering terminal
  buffer, keyboard input, resize, cursor tracking
- desktop/widgets/terminal_layout.py: Split pane manager + focus tracking
- desktop/widgets/suggestion_bar.py: Bottom bar showing one suggestion
  at a time with Tab/Esc accept/decline
- desktop/widgets/transcribe_pane.py: Live whisper transcription pane
- desktop/suggestion.py: Suggestion queue engine + error detection
  (command not found, npm errors, tracebacks, git/permission errors)
- desktop/screens/desktop_screen.py: Full desktop environment replacing
  MainScreen — terminal layout + Merlin HUD sidebar + suggestion bar
- app.py: Switched from MainScreen to DesktopScreen

Architecture:
- Center: terminal multiplexer (Alt+H/V split, Alt+W close, Alt+Tab focus)
- Right: Merlin chat sidebar with HUD (mode, listening, suggestions)
- Bottom: suggestion bar (one at a time, Tab=accept, Esc=decline)
- Background transparent, non-essential HUD elements hidden
- Suggestions auto-detect errors from terminal output and queue fixes
- !command types into focused terminal

Files created:
- desktop/terminal/pty.py, emulator.py, __init__.py
- desktop/widgets/terminal_pane.py, terminal_layout.py
- desktop/widgets/suggestion_bar.py, transcribe_pane.py
- desktop/suggestion.py
- desktop/screens/desktop_screen.py

Modified:
- desktop/app.py (DesktopScreen replaces MainScreen)
- desktop/screens/launch_screen.py (pushes "desktop" instead of "main")

- Sign: Claude

- Sign: Claude

- Sign: Claude
